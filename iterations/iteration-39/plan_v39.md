# Implementation Plan: env Module (Iteration 39)

**Module**: `env` (`opencode-core`)
**Source**: `opencode-rust/crates/core/src/env.rs`
**Last Updated**: 2026-04-21
**Overall Status**: ⚠️ 97% Complete — One P1 gap remaining

---

## 1. Priority Classification

### P0 — Blocking Issues
**None** — No blocking issues.

### P1 — Must Fix (Current Sprint)
| Gap | Description | Impact | Fix |
|-----|-------------|--------|-----|
| FR-403.9 | Missing `instance_isolation_does_not_affect_process_env` test | 88.9% → 100% test coverage | Add test verifying `std::env::var("ISOLATION_TEST")` returns `Err` after `env.set()` |

### P2 — Technical Debt (Future Sprint)
| Item | Description | Impact | Remediation |
|------|-------------|--------|-------------|
| `#[allow(dead_code)]` on `EnvManager` | Struct marked but never used outside module | Compiler warning | Add actual use point in subprocess env propagation, or remove suppression if truly internal |
| `#[allow(dead_code)]` on `impl` block | Impl block marked but all methods used internally | Compiler warning | Remove suppression, add use points, or document why kept |

---

## 2. Implementation Roadmap

### Phase 1: Fix P1 Gap (Current Sprint)
- [ ] **P1**: Add `test_env_instance_isolation` test to `env.rs`

### Phase 2: Address Technical Debt (Future Sprint)
- [ ] **P2**: Review `#[allow(dead_code)]` — either add use points or remove suppression
- [ ] **P2**: Add documentation example to `EnvManager` if kept

---

## 3. P1 Implementation Detail

### FR-403.9: Missing Instance Isolation Test

**Test Location**: `opencode-rust/crates/core/src/env.rs` → `#[cfg(test)] mod tests`

**Required Test**:
```rust
#[test]
fn test_env_instance_isolation() {
    let env = EnvManager::new();

    // Set a variable on the EnvManager instance
    env.set("ISOLATION_TEST".into(), "isolated".into());

    // Verify it's accessible via EnvManager
    assert_eq!(env.get("ISOLATION_TEST"), Some("isolated".into()));

    // Verify std::env is NOT affected
    assert!(std::env::var("ISOLATION_TEST").is_err());
}
```

**Verification**:
```bash
cd opencode-rust && cargo test -p opencode-core test_env_instance_isolation
```

**Expected Outcome**: Test passes, confirming instance isolation works correctly.

---

## 4. Success Criteria

| Criterion | Target | Current |
|-----------|--------|---------|
| Test coverage | 100% (9/9) | 88.9% (8/9) |
| P0 blocking issues | 0 | 0 |
| P1 gaps | 0 | 1 |
| P2 technical debt items | 0 | 2 |

---

## 5. Risk Assessment

| Risk | Probability | Impact | Mitigation |
|------|-------------|--------|------------|
| Test reveals std::env mutation bug | Low | High | Unlikely — implementation follows spec correctly |
| `#[allow(dead_code)]` removal breaks build | Low | Medium | Add use points first before removing suppression |

---

## 6. Dependencies

- None — this is a standalone test addition with no external dependencies.

---

## 7. Verification Checklist

After P1 implementation:
- [ ] `test_env_instance_isolation` test exists in `env.rs`
- [ ] Test passes: `cargo test -p opencode-core test_env_instance_isolation`
- [ ] All 9 tests pass: `cargo test -p opencode-core env::tests`
- [ ] No new warnings introduced
- [ ] Gap analysis updated to reflect 100% coverage

---

*Plan generated by Sisyphus gap analysis pipeline*
*FR numbers: FR-390 to FR-405 (aligned to iteration 39)*